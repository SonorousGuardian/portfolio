---
/* We don't need the generic CallToAction anymore, we will build a better one here */
---

<aside>
	<h2>Looking for a GRC Analyst?</h2>
	
	<a href="mailto:amriteshshree@gmail.com" class="gmail-btn">
		<i class="fa-solid fa-envelope"></i> Send Me an Email
	</a>
</aside>

<style>
	aside {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 3rem;
		border-top: 1px solid var(--gray-800);
		border-bottom: 1px solid var(--gray-800);
		padding: 5rem 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
		/* Subtle top glow */
		border-top-color: var(--neon-cyan);
	}

	h2 {
		font-size: var(--text-xl);
		text-align: center;
		max-width: 15ch;
		color: var(--gray-0);
	}

	/* --- THE GMAIL BUTTON --- */
	.gmail-btn {
		display: inline-flex;
		align-items: center;
		gap: 0.8rem; /* Space between icon and text */
		
		background-color: var(--neon-cyan);
		color: black; /* Black text on Cyan background is high contrast */
		font-weight: 700;
		text-decoration: none;
		
		padding: 1rem 2.5rem;
		border-radius: 999rem; /* Pill shape */
		font-size: 1.2rem;
		
		/* The Glow Effect */
		box-shadow: 0 0 15px rgba(0, 243, 255, 0.4);
		transition: all 0.3s ease;
	}

	.gmail-btn i {
		font-size: 1.4rem; /* Make the icon slightly larger */
	}

	/* Hover State */
	.gmail-btn:hover {
		transform: translateY(-3px); /* Moves up slightly */
		box-shadow: 0 0 25px rgba(0, 243, 255, 0.7); /* Brighter glow */
		background-color: #fff; /* Turns white on hover for effect */
	}

	@media (min-width: 50em) {
		aside {
			padding: 7.5rem;
			flex-direction: row;
			flex-wrap: wrap;
			justify-content: space-between;
		}

		h2 {
			font-size: var(--text-3xl);
			text-align: left;
		}
	}
</style>